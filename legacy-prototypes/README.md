# Legacy Prototypes Archive

This directory is reserved for old prototype artifacts (sales landing pages, standalone demos, prior class-platform remnants).

Current status:
- Referenced files from `/workspace/output/business-class-app/*` are not present in this machine/workspace.
- If recovered later, place them here and keep production code isolated in `apps/web`.
